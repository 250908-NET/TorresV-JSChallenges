//Use the array `.map()` method and an arrow function to create a new array of
//**squares** of these numbers:
const nums = [1, 2, 3, 4, 5];

// Use map with arrow function to get squares
const squares = nums.map(n => n * n);

console.log(squares); // [1, 4, 9, 16, 25]