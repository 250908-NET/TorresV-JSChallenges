//Write an arrow function named `makeBook` that takes two parameters 
//— `title` and `author`

// Arrow function that returns an object
const makeBook = (title, author) => ({ title, author });

// Test it
console.log(makeBook("Kinito Mendez", "Kiara Calderon"));
console.log(makeBook("Anacaona", "Viviana Torres"));
